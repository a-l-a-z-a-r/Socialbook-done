apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-datasources
data:
  datasources.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: {{ .Values.grafana.prometheusUrl }}
        isDefault: true
        editable: true
